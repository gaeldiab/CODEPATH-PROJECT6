<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/pet_image"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/pet_id"
        style="@style/id_text"
        android:layout_width="244dp"
        android:layout_height="41dp"
        android:fontFamily="@font/aclonica"
        android:text="Cute dogs"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/pet_image" />

    <TextView
        android:id="@+id/pet_url"
        android:layout_width="244dp"
        android:layout_height="44dp"
        android:textAppearance="@style/TextAppearance.pic"
        android:text="Shibas"
        style="@style/url_text"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/pet_id" />

</androidx.constraintlayout.widget.ConstraintLayout>